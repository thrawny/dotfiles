#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "--version" ]]; then
  exec /home/thrawny/dotfiles/bin/sandbox claude "$@"
fi

exec /home/thrawny/dotfiles/bin/sandbox direnv exec "$PWD" claude "$@"
