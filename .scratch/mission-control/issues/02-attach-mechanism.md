# Attach mechanism

Type: prototype
Status: open

## Question

What is the default attach verb, concretely: **zmx attach-in-place** (the terminal you're in becomes the thread), **window summon** (spawn/focus a dedicated ghostty window for the thread), or both? Build throwaway prototypes of each in ghostty + niri and decide by feel.

Also resolve while prototyping: what detach feels like (and what the freed terminal shows), and what "pin to workspace" does mechanically for heavy engagements (deliberately-open charting decision).
