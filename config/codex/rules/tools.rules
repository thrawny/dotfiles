# General tools - allow these to run without prompting

prefix_rule(
    pattern=["grep"],
    decision="allow",
    justification="Search text"
)
prefix_rule(
    pattern=["find"],
    decision="allow",
    justification="Find files"
)
prefix_rule(
    pattern=["cat"],
    decision="allow",
    justification="Read files"
)

prefix_rule(
    pattern=["go", "build"],
    decision="allow",
    justification="Go build"
)
prefix_rule(
    pattern=["go", "test"],
    decision="allow",
    justification="Go test"
)
prefix_rule(
    pattern=["go", "mod", "tidy"],
    decision="allow",
    justification="Go mod tidy"
)
prefix_rule(
    pattern=["go", "doc"],
    decision="allow",
    justification="Go doc"
)

prefix_rule(
    pattern=["gh", "pr", "view"],
    decision="allow",
    justification="GitHub PR view"
)
prefix_rule(
    pattern=["gh", "pr", "checks"],
    decision="allow",
    justification="GitHub PR checks"
)
prefix_rule(
    pattern=["gh", "pr", "list"],
    decision="allow",
    justification="GitHub PR list"
)
prefix_rule(
    pattern=["gh", "pr", "create"],
    decision="allow",
    justification="GitHub PR create"
)
prefix_rule(
    pattern=["gh", "run", "view"],
    decision="allow",
    justification="GitHub run view"
)
prefix_rule(
    pattern=["gh", "run", "list"],
    decision="allow",
    justification="GitHub run list"
)
prefix_rule(
    pattern=["gh", "issue", "view"],
    decision="allow",
    justification="GitHub issue view"
)
prefix_rule(
    pattern=["gh", "issue", "list"],
    decision="allow",
    justification="GitHub issue list"
)

prefix_rule(
    pattern=["zmx", "kill"],
    decision="allow",
    justification="zmx kill"
)
prefix_rule(
    pattern=["zmx", "wait"],
    decision="allow",
    justification="zmx wait"
)
prefix_rule(
    pattern=["zmx", "history"],
    decision="allow",
    justification="zmx history"
)
prefix_rule(
    pattern=["zmx", "list"],
    decision="allow",
    justification="zmx list"
)
prefix_rule(
    pattern=["zmx", "detach"],
    decision="allow",
    justification="zmx detach"
)

prefix_rule(
    pattern=["git", "log"],
    decision="allow",
    justification="Git log"
)

prefix_rule(
    pattern=["notify"],
    decision="allow",
    justification="Notify"
)
prefix_rule(
    pattern=["linear", "issues"],
    decision="allow",
    justification="Linear issues"
)
prefix_rule(
    pattern=["linear", "start"],
    decision="allow",
    justification="Linear start"
)
prefix_rule(
    pattern=["remove-handoff"],
    decision="allow",
    justification="Remove handoff"
)
prefix_rule(
    pattern=["ast-grep"],
    decision="allow",
    justification="ast-grep"
)
prefix_rule(
    pattern=["sg"],
    decision="allow",
    justification="ast-grep (sg)"
)
