# Code quality tools - allow these to run without prompting

# Go
prefix_rule(
    pattern=["golangci-lint"],
    decision="allow",
    justification="Go linting and formatting"
)

# Python
prefix_rule(
    pattern=["ruff"],
    decision="allow",
    justification="Python linting and formatting"
)
prefix_rule(
    pattern=["basedpyright"],
    decision="allow",
    justification="Python type checking"
)

# Rust
prefix_rule(
    pattern=["cargo", "fmt"],
    decision="allow",
    justification="Rust formatting"
)
prefix_rule(
    pattern=["cargo", "clippy"],
    decision="allow",
    justification="Rust linting"
)
prefix_rule(
    pattern=["cargo", "check"],
    decision="allow",
    justification="Rust type checking"
)
prefix_rule(
    pattern=["cargo", "build"],
    decision="allow",
    justification="Rust build"
)
prefix_rule(
    pattern=["cargo", "test"],
    decision="allow",
    justification="Rust tests"
)

# TypeScript/JavaScript
prefix_rule(
    pattern=["biome"],
    decision="allow",
    justification="TypeScript/JavaScript linting and formatting"
)
prefix_rule(
    pattern=["eslint"],
    decision="allow",
    justification="JavaScript/TypeScript linting"
)
prefix_rule(
    pattern=["prettier"],
    decision="allow",
    justification="Code formatting"
)

# Nix
prefix_rule(
    pattern=["nixfmt"],
    decision="allow",
    justification="Nix formatting"
)
prefix_rule(
    pattern=["statix"],
    decision="allow",
    justification="Nix linting"
)

# General build tools
prefix_rule(
    pattern=["just"],
    decision="allow",
    justification="Task runner"
)
prefix_rule(
    pattern=["make"],
    decision="allow",
    justification="Build tool"
)
