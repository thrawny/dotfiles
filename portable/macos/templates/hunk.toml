agent_notes = true
line_numbers = true
mode = "stack"
prompt_save_view_preferences = false
theme = "custom"
transparent_background = false
wrap_lines = true

[custom_theme]
accent = "{{diff.changed.foreground}}"
accentMuted = "{{applications.hunk.accentMuted}}"
addedBg = "{{diff.added.lineBackground}}"
addedContentBg = "{{diff.added.emphasisBackground}}"
addedSignColor = "{{diff.added.foreground}}"
background = "{{semantic.background}}"
badgeAdded = "{{diff.added.foreground}}"
badgeNeutral = "{{diff.changed.foreground}}"
badgeRemoved = "{{diff.removed.foreground}}"
base = "graphite"
border = "{{semantic.border}}"
contextBg = "{{semantic.background}}"
contextContentBg = "{{semantic.background}}"
fileDeleted = "{{diff.removed.foreground}}"
fileModified = "{{semantic.accentAlt}}"
fileNew = "{{diff.added.foreground}}"
fileRenamed = "{{diff.changed.foreground}}"
fileUntracked = "{{semantic.warning}}"
label = "Monokai Spectrum"
lineNumberBg = "{{semantic.background}}"
lineNumberFg = "{{applications.hunk.muted}}"
movedAddedBg = "{{diff.added.dimBackground}}"
movedRemovedBg = "{{diff.removed.dimBackground}}"
muted = "{{applications.hunk.muted}}"
noteBackground = "{{applications.hunk.noteBackground}}"
noteBorder = "{{semantic.accentAlt}}"
noteTitleBackground = "{{applications.hunk.noteTitleBackground}}"
noteTitleText = "{{semantic.foreground}}"
panel = "{{semantic.background}}"
panelAlt = "{{applications.hunk.panelAlt}}"
removedBg = "{{diff.removed.lineBackground}}"
removedContentBg = "{{diff.removed.emphasisBackground}}"
removedSignColor = "{{diff.removed.foreground}}"
selectedHunk = "{{applications.hunk.selectedHunk}}"
text = "{{semantic.foreground}}"

[custom_theme.syntax]
comment = "{{syntax.comment}}"
default = "{{syntax.variable}}"
function = "{{syntax.function}}"
keyword = "{{syntax.keyword}}"
number = "{{syntax.number}}"
property = "{{syntax.number}}"
punctuation = "{{applications.hunk.punctuation}}"
string = "{{syntax.string}}"
type = "{{syntax.type}}"
