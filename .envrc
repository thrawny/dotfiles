# Activate nix devshell if nix-direnv is available
# Provides: just, cargo, rustc, formatters, linters, and GTK libs (Linux only)
if has use_flake; then
  use flake ./nix
fi

# Python environment via uv
layout uv
