# vim:ft=ansible:

# - name: vundle repo is present
#   git:
#     repo: https://github.com/VundleVim/Vundle.vim.git
#     dest: "{{ dotfiles_dir }}/vim/bundle/Vundle.vim"
# 
# - name: vim plugins are installed
#   command: "vim +PluginInstall +qall"
