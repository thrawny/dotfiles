[project]
name = "dotfiles"
version = "0.1.0"
description = "Personal dotfiles repository with Ansible automation for macOS and Ubuntu"
requires-python = ">=3.12"
dependencies = [
    "ansible>=10.0.0",
    "claude-code-sdk>=0.0.20",
    "fastmcp>=2.11.1",
    "requests>=2.32.4",
]

[tool.uv]
dev-dependencies = ["ansible-lint>=25.6.1", "ruff>=0.12.9"]
