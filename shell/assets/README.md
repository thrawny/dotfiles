Provider logo geometry is copied from Quotabar's `assets/claude.svg` and
`assets/openai.svg`, originally from LobeHub's MIT-licensed icon collection.
The SVGs retain `currentColor`; `ProviderIcon.qml` applies the central theme.
